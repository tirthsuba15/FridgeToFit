# ENVIRONMENT VARIABLES

*List all required keys here. DO NOT include actual values.*
*Format: [VAR_NAME] — [description] — [where to get it]*

---

## Required Keys
- `JWT_SECRET` — secret for auth tokens — generated by P1
- `DB_PATH` — path to sqlite file (default: ./server/db/dev.db) — P2
- `ANTHROPIC_API_KEY` — for Claude integration — P4
- `PORT` — server port (default: 3001)

<!-- add keys below as needed -->
